name: deploy
run-name: Open infrastructure PR for ${{ github.event.release.tag_name }}

on:
  release:
    types: [published]

env:
  INFRA_REPO: Gaucho-Racing/infrastructure
  KUSTOMIZATION: infra/kubernetes/gr-foundry/manifests/workbench/kustomization.yaml

jobs:
  infra-pr:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout Workbench (full history + tags)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Pull secrets
        id: vault
        uses: Gaucho-Racing/vault-pull-secrets@v1
        with:
          secrets: infrastructure.pr_token

      - name: Checkout infrastructure
        uses: actions/checkout@v4
        with:
          repository: ${{ env.INFRA_REPO }}
          token: ${{ fromJSON(steps.vault.outputs.secrets_json).PR_TOKEN }}
          path: infra

      - name: Resolve versions
        id: versions
        run: |
          NEW_TAG="${{ github.event.release.tag_name }}"
          NEW="${NEW_TAG#v}"
          OLD="$(yq '.images[] | select(.name == "ghcr.io/gaucho-racing/workbench-server") | .newTag' "$KUSTOMIZATION")"
          echo "new_tag=$NEW_TAG" >> "$GITHUB_OUTPUT"
          echo "new=$NEW" >> "$GITHUB_OUTPUT"
          echo "old=$OLD" >> "$GITHUB_OUTPUT"
          echo "short_sha=$(git rev-parse --short "$GITHUB_SHA")" >> "$GITHUB_OUTPUT"
          if [ "$NEW" = "$OLD" ]; then
            echo "infra already at $NEW; nothing to do."
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Bump image tags
        if: steps.versions.outputs.skip == 'false'
        env:
          NEW: ${{ steps.versions.outputs.new }}
        run: |
          awk -v new="$NEW" '
            $0 ~ "- name: ghcr.io/gaucho-racing/workbench-(server|web)$" { inworkbench=1; print; next }
            inworkbench==1 && $1=="newTag:" { sub(/newTag:[[:space:]]*.*/, "newTag: " new); inworkbench=0 }
            { print }
          ' "$KUSTOMIZATION" > "$KUSTOMIZATION.tmp" && mv "$KUSTOMIZATION.tmp" "$KUSTOMIZATION"
          echo "--- diff ---"
          git -C infra diff -- kubernetes/gr-foundry/manifests/workbench/kustomization.yaml

      - name: Build pull request body
        if: steps.versions.outputs.skip == 'false'
        env:
          NEW_TAG: ${{ steps.versions.outputs.new_tag }}
        run: |
          {
            echo "- Bump \`workbench-server\` to \`${NEW_TAG#v}\`"
            echo "- Bump \`workbench-web\` to \`${NEW_TAG#v}\`"
            echo "- Release: https://github.com/${{ github.repository }}/releases/tag/$NEW_TAG"
          } > "$RUNNER_TEMP/pr-body.txt"

      - name: Create pull request
        if: steps.versions.outputs.skip == 'false'
        working-directory: infra
        env:
          GH_TOKEN: ${{ fromJSON(steps.vault.outputs.secrets_json).PR_TOKEN }}
          NEW: ${{ steps.versions.outputs.new }}
          NEW_TAG: ${{ steps.versions.outputs.new_tag }}
          SHORT_SHA: ${{ steps.versions.outputs.short_sha }}
          RELEASE_AUTHOR: ${{ github.event.release.author.login }}
        run: |
          BRANCH="bot/bump-workbench-${NEW}"
          git config user.name "GR Admin"
          git config user.email "team@gauchoracing.com"
          git switch -C "$BRANCH"
          git commit -am "Deploy: workbench to ${NEW_TAG} (${SHORT_SHA})"
          git push -f -u origin "$BRANCH"
          PR="$(gh pr list --repo "$INFRA_REPO" --head "$BRANCH" --state open --json number -q '.[0].number')"
          if [ -n "$PR" ]; then
            echo "PR #$PR already open for $BRANCH; pushed the updated branch."
          else
            gh pr create \
              --repo "$INFRA_REPO" \
              --base main \
              --head "$BRANCH" \
              --title "Deploy: workbench to ${NEW_TAG} (${SHORT_SHA})" \
              --body-file "$RUNNER_TEMP/pr-body.txt"
            PR="$(gh pr list --repo "$INFRA_REPO" --head "$BRANCH" --state open --json number -q '.[0].number')"
          fi
          if [ -n "$PR" ] && [ -n "$RELEASE_AUTHOR" ]; then
            gh pr edit "$PR" --repo "$INFRA_REPO" --add-reviewer "$RELEASE_AUTHOR" \
              || echo "Could not request review from @$RELEASE_AUTHOR (they may be the PR author)."
          fi
